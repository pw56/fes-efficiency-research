# 文化祭の運営の効率化に関する研究

## 概要
文化祭の運営の効率化に関する研究のレポート。

## 推奨環境
- LaTeXエンジン: LuaLaTeX (TeX Live 2025以降)
<!-- - 主要パッケージ: `tcolorbox`, `amsmath` -->

## ビルド方法
ビルドには`Latexmk`と`LuaLaTeX`を使用します。以下のコマンドを実行してください。

```bash
latexmk -lualatex main.tex
```
